version: '3.8'
services:
  network-monitoring-service:
    build:
      context: .
      dockerfile: docker/Dockerfile
    ports:
      - "8080:8080"
    environment:
      - MONITORING_SIMULATION_MODE=true
